tasks :

Does the project handle HTTP status 400 - Bad Requests?
+Does the project present a script to build the images and containers? (using a script to simplify the build)





diablo :
- comments likes and dislikes.
- categories filtring.


anass :
- post can have multiple categories.
- add \# for better search indexing.

- styling.
- backend forms.
- remove token from sessions in db.

hamza :
- Dockerfile.
- script to run the project (I should provide a template).

mellaghi :
- upload image.

yassin :
- backend testing.
- auth bonus (after validating forum).